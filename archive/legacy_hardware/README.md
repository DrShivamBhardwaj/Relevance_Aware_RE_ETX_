# Legacy host-run snapshot

This directory preserves a pre-held-out-revision host-run snapshot for provenance only. Its metric naming and operating point predate the corrected two-level influence accounting and the final relay-pressure coefficient 5.0. It must not be used for current manuscript claims. Current evidence is documented under `results/`, `validation/statistics/`, `validation/control_plane/`, and `reproducibility/`.
