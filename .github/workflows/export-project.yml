name: Export complete project snapshot
on:
  push:
    branches: [artifact-export]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Build repository archive
        run: |
          git archive --format=zip --output=PROJECT_REPOSITORY_SNAPSHOT_08563b9.zip HEAD
          sha256sum PROJECT_REPOSITORY_SNAPSHOT_08563b9.zip > PROJECT_REPOSITORY_SNAPSHOT_08563b9.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: project-repository-snapshot-08563b9
          path: |
            PROJECT_REPOSITORY_SNAPSHOT_08563b9.zip
            PROJECT_REPOSITORY_SNAPSHOT_08563b9.sha256
          compression-level: 0
          retention-days: 7
